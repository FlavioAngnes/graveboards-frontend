import { BeatmapThumbnailFallbackDirective } from './beatmap-thumbnail-fallback.directive';

describe('BeatmapThumbnailFallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new BeatmapThumbnailFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
